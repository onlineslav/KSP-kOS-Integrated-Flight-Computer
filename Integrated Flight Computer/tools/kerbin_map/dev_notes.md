Desired features:
- finish implementing the 3d mode. I want waypoints, etc. visible in 3d. This was partially implemented by Codex but it is janky. the map doesn't move underneath, which it should.
  - Also, curious if there is a way to show the map in 3d - i.e.: having elevation of the terrain visible in 3d on the map. I don't know what this would require.
- Show/Hide all button under layers
  
- ability to load CSV logs so we can replay flights
- ability to plan flight plans and save them so they can be opened with the IFC terminal/GUI
  - needs work on the IFC-side. Currently no way to save or load flight plans.